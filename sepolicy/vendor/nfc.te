# Allow nfc to find hal_wiredse_hwservice
allow nfc nxpese_hwservice:hwservice_manager find;

# Allow nfc to get vendor_nfc_prop
get_prop(nfc, oppo_vendor_prop)

allow nfc vendor_nfc_vendor_data_file:dir search;
