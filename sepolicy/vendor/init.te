# Allow init to create vl53l1_socket
allow init vl53l1_socket:sock_file create_file_perms;

allow init oppo_reserve:dir mounton;
allow init vendor_data_file:dir mounton;

allow init sysfs_vibrator:lnk_file rw_file_perms;
allow init init:capability { sys_nice };

# Allow init to read and write to proc_oppo_extra
allow init proc_oppo_extra:dir r_dir_perms;
allow init proc_oppo_extra:file rw_file_perms;

# Allow init to modify vendor props
allow init {
engineer_vendor_prop
oppo_vendor_prop }:file { relabelto write read map };

allow init adsprpcd_file:file mounton;
